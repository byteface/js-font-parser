// a simple utility class for colorColor = Class.extend({		// set black and white as defaults		range:["#000000","#ffffff"],        init: function(  )		{		}		// returns a completley random color		, rndColor: function() {		    return '#' + ('00000' + (Math.random() * 16777216 << 0).toString(16)).substr(-6);		}							// returns a random color from the range you set		, rndColorFromPallete: function() {		    return this.range[ Math.round( Math.random()*this.range.length-1 ) ];		}				, setPallete: function( array ) {		    this.range = array;		}						});